import React from "react";

const AppBar = (props) => {
    console.log("props", props)
    return (
    <>
       <div>
          <h2>AppBar</h2>
       </div>
    </>
    );
};

export default AppBar;